apiVersion: "security.istio.io/v1beta1"
kind: "AuthorizationPolicy"
metadata:
  name: ccui-allow-access
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: cc-ui
  action: ALLOW
  rules:
    - {}  
