apiVersion: "security.istio.io/v1beta1"
kind: "AuthorizationPolicy"
metadata:
  name: vision-allow-access
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: vision
  action: ALLOW
  rules:
    - {}  
